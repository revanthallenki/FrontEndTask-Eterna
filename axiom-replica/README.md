# Axiom-Trade Replica

Run:
1. npm install
2. npm run dev
3. Open http://localhost:3000

This repo contains a pixel-focused Token Discovery Table replica using Next.js 14, TypeScript, Tailwind CSS, Redux Toolkit and React Query. See `src/` for files.

